/**
 * __Tests whether doc comment markdown is preserved__
 * 
 * - foo
 * - bar
 * - baz
 * - hoge
 *     - hogera
 *     - hogehoge
 * - piyo
 * - fuga
 * 
 * ```javascript
 * namespace Foo {
 *   class Bar {
 *     baz() {
 *       return () => {
 *         const x = 9;
 *         return x;
 *       }
 *     }
 *   }
 * }
 * ```
 */
({
})