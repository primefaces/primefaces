declare namespace PrimeFaces.widget {
    /**
     * Tests that no return tag is required when an arrow function inside the method returns.
     */
    export class MethodReturnWithinArrowFunction {
        /**
         * method foo
         */
        foo(): void;
    }
}