declare namespace PrimeFaces.widget {
    /**
     * Tests that no return tag is required when a method of a named class inside the method returns.
     */
    export class MethodReturnWithinNamedClassMethod {
        /**
         * method foo
         */
        foo(): void;
    }
}