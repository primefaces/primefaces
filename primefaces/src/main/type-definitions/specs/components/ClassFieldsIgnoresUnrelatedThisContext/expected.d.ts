declare namespace PrimeFaces.widget {
    /**
     * Tests that a property accessed via a different this context is ignored.
     */
    export class ClassFieldsIgnoresUnrelatedThisContext {
        met1(): void;
    }
}