declare namespace PrimeFaces.widget {
    /**
     * Tests that no return tag is required when a named function inside the method returns.
     */
    export class MethodReturnWithinNamedFunction {
        /**
         * method foo
         */
        foo(): void;
    }
}