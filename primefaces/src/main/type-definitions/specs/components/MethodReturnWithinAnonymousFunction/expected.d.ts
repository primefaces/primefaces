declare namespace PrimeFaces.widget {
    /**
     * Tests that no return tag is required when an anonymous function inside the method returns.
     */
    export class MethodReturnWithinAnonymousFunction {
        /**
         * method foo
         */
        foo(): void;
    }
}