declare namespace PrimeFaces.widget {
    /**
     * Tests that no return tag is required when a method of an anonymous class inside the method returns.
     */
    export class MethodReturnWithinAnonymousClassMethod {
        /**
         * method foo
         */
        foo(): void;
    }
}