import autosize from "autosize";

import "./forms.inputtextarea.widget.js";

// Expose autosize to the global scope
// Not needed for our code, but may already be used by external code
Object.assign(window, { autosize });
