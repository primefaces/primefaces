/**
 * Tests a visibility modifier on a class created from a prop
 * @class cfg class cfg
 * @prop {PropSubCreateClassVisibility.Config} cfg prop cfg
 * @prop {string} cfg.bar prop bar
 * @private cfg.bar
 */
({
})