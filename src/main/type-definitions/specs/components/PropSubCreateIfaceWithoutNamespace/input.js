/**
 * Tests the ability to create a type without namespace for a prop
 * 
 * @interface bar iface bar
 * @prop {IfaceWithoutNamespace} bar prop bar
 * @prop {string} bar.x prop bar-x
 * @prop {number} bar.y prop bar-y
 */
({
})