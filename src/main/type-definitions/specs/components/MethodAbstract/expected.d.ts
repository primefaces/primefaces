declare namespace PrimeFaces.widget {
    /**
     * Tests the abstract modifier on a method and on a class
     */
    export abstract class MethodAbstract {
        /**
         * method bar
         */
        abstract bar(): void;
        /**
         * method foo
         */
        abstract foo(): void;
    }
}