/**
 * Tests export a property as an additional const
 * 
 * @prop {number} hoge prop hoge
 * @constant {PrimeFaces.widget.exportConstantPropHoge} hoge const hoge
 */
({
    /**
     * prop bar
     * @constant {PrimeFaces.widget.exportConstantPropBar} . const bar
     * @type {string}
     */
    bar: "42",
})