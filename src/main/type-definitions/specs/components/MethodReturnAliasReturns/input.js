/**
 * Tests that <AT>returns works as well
 * @method bar method bar
 * @returns {string} bar bar retval
 */
({
    /**
     * method foo
     * @returns {RegExp} foo retval
     */
    foo() {
        return /foo/;
    }
})