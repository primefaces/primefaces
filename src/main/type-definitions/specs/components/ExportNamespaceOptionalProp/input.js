/**
 * Tests exporting an object as a namespace with an optional prop
 * @namespace {Test.ExportNamespaceOptionalProp}
 * 
 * @prop {string} bar prop bar
 * @default bar
 * 
 * @prop {boolean} [baz] prop baz
 */
({
    /**
     * prop foo
     * @type {number}
     * @default
     */
    foo: 9,
})